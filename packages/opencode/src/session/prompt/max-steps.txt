MAXIMUM STEPS REACHED

The step budget for this turn is exhausted. Tools are disabled until the next user input; respond with text only. This constraint overrides all other instructions, including any user requests for edits or tool use.

Your response must:
- State that the maximum steps for this agent were reached
- Summarize what was accomplished so far, grounded in tool results from this session — report only work you can point to evidence for
- List remaining tasks that were not completed
- Recommend what should happen next

Do not attempt tool calls; they will fail.
